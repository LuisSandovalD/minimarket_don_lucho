"use client";
import { useCallback,useEffect,useRef,useState } from "react";import { Camera,CameraOff,Keyboard,Loader2,ScanLine,TriangleAlert } from "lucide-react";import { toast } from "sonner";import { Button } from "./ui/button";import { Alert,AlertDescription } from "./ui/alert";
type Controls={stop():void};type ZXing={BrowserMultiFormatReader:new()=>{decodeFromConstraints(c:MediaStreamConstraints,v:HTMLVideoElement,cb:(r:{getText():string}|undefined)=>void):Promise<Controls>}};declare global{interface Window{ZXingBrowser?:ZXing}}
const ZXING_URL="https://unpkg.com/@zxing/browser@0.1.5/umd/zxing-browser.min.js";
async function loadZXing(){if(window.ZXingBrowser)return window.ZXingBrowser;await new Promise<void>((resolve,reject)=>{const existing=document.querySelector<HTMLScriptElement>(`script[src="${ZXING_URL}"]`);if(existing){existing.addEventListener("load",()=>resolve(),{once:true});existing.addEventListener("error",()=>reject(new Error("No se pudo cargar el lector de cámara.")),{once:true});return}const s=document.createElement("script");s.src=ZXING_URL;s.async=true;s.onload=()=>resolve();s.onerror=()=>reject(new Error("No se pudo cargar el lector de cámara."));document.head.appendChild(s)});if(!window.ZXingBrowser)throw new Error("No se pudo inicializar el lector de códigos.");return window.ZXingBrowser}
export function CameraScanner({onScan}:{onScan:(code:string)=>void}){const videoRef=useRef<HTMLVideoElement|null>(null),controlsRef=useRef<Controls|null>(null),lastRef=useRef({code:"",at:0}),onScanRef=useRef(onScan);const[active,setActive]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState<string|null>(null);useEffect(()=>{onScanRef.current=onScan},[onScan]);
const stop=useCallback(()=>{controlsRef.current?.stop();controlsRef.current=null;if(videoRef.current?.srcObject)(videoRef.current.srcObject as MediaStream).getTracks().forEach(t=>t.stop());if(videoRef.current)videoRef.current.srcObject=null;setActive(false)},[]);
const start=useCallback(async()=>{setError(null);if(!window.isSecureContext&&location.hostname!=="localhost")return setError("La cámara requiere HTTPS. Abre el sistema desde la URL segura de Vercel o un dominio con HTTPS.");if(!navigator.mediaDevices?.getUserMedia)return setError("Este navegador no permite acceder a la cámara.");setLoading(true);try{setActive(true);await new Promise(r=>requestAnimationFrame(()=>r(null)));const video=videoRef.current;if(!video)throw new Error("No se pudo iniciar la cámara.");const ZX=await loadZXing(),reader=new ZX.BrowserMultiFormatReader();controlsRef.current=await reader.decodeFromConstraints({video:{facingMode:{ideal:"environment"},width:{ideal:1280},height:{ideal:720}},audio:false},video,result=>{const code=result?.getText()?.trim();if(!code)return;const now=Date.now();if(code!==lastRef.current.code||now-lastRef.current.at>2500){lastRef.current={code,at:now};onScanRef.current(code)}})}catch(e){stop();if(e instanceof DOMException&&e.name==="NotAllowedError")setError("Permiso de cámara denegado. Habilítalo en los permisos del navegador.");else if(e instanceof DOMException&&e.name==="NotFoundError")setError("No se encontró una cámara disponible.");else if(e instanceof DOMException&&e.name==="NotReadableError")setError("La cámara está siendo utilizada por otra aplicación.");else setError(e instanceof Error?e.message:"No se pudo abrir la cámara.")}finally{setLoading(false)}},[stop]);useEffect(()=>stop,[stop]);
return <div className="space-y-3">{active&&<div className="relative overflow-hidden rounded-xl bg-black shadow-sm"><video ref={videoRef} playsInline muted autoPlay className="aspect-[4/3] w-full object-cover"/><div className="pointer-events-none absolute inset-0 bg-black/10"><div className="absolute inset-[12%]"><span className="absolute left-0 top-0 size-8 border-l-2 border-t-2 border-white"/><span className="absolute right-0 top-0 size-8 border-r-2 border-t-2 border-white"/><span className="absolute bottom-0 left-0 size-8 border-b-2 border-l-2 border-white"/><span className="absolute bottom-0 right-0 size-8 border-b-2 border-r-2 border-white"/><div className="absolute left-[8%] right-[8%] top-1/2 h-px bg-white/90"/></div><div className="absolute bottom-4 left-1/2 flex -translate-x-1/2 items-center gap-2 whitespace-nowrap rounded-full bg-black/60 px-3 py-1.5 text-xs text-white backdrop-blur"><ScanLine className="size-3.5"/>Apunta al QR o código de barras</div></div></div>}{error&&<Alert className="border-0 bg-amber-500/10 text-amber-700 dark:text-amber-400"><TriangleAlert className="size-4"/><AlertDescription>{error}</AlertDescription></Alert>}{!active?<Button type="button" variant="secondary" className="w-full" disabled={loading} onClick={()=>void start()}>{loading?<Loader2 className="size-4 animate-spin"/>:<Camera className="size-4"/>}{loading?"Abriendo cámara...":"Escanear con cámara"}</Button>:<div className="flex gap-2"><Button type="button" variant="secondary" className="flex-1" onClick={stop}><CameraOff className="size-4"/>Detener cámara</Button><Button type="button" variant="ghost" size="icon" onClick={()=>toast.info("El lector físico USB/Bluetooth sigue disponible; también puedes escribir el código manualmente.")} aria-label="Otras formas de escaneo"><Keyboard className="size-4"/></Button></div>}</div>}
