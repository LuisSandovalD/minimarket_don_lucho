import Link from "next/link";import { requireUser } from "@/lib/auth";import { WalletCards,Banknote,CircleDollarSign,ArrowRight } from "lucide-react";
export const dynamic="force-dynamic";
export default async function FinancePage(){const user=await requireUser();const actions=[{label:"Caja",description:"Apertura, cierre y control de efectivo.",href:"/cash",icon:Banknote,permission:"cash.view"},{label:"Gastos",description:"Registra y consulta egresos del negocio.",href:"/expenses",icon:CircleDollarSign,permission:"expenses.view"}].filter(x=>user.permissions.includes(x.permission));return <div className="w-full space-y-6"><div className="space-y-1"><div className="flex items-center gap-2"><WalletCards className="size-5 text-muted-foreground"/><h1 className="text-2xl font-semibold tracking-tight sm:text-3xl">Finanzas</h1></div><p className="text-sm text-muted-foreground">Caja y gastos reunidos en un solo módulo.</p></div><section className="grid gap-3 sm:grid-cols-2 xl:max-w-3xl">{actions.map(({label,description,href,icon:Icon})=><Link key={href} href={href} className="group rounded-2xl bg-card p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between"><div className="flex size-11 items-center justify-center rounded-xl bg-muted"><Icon className="size-5"/></div><ArrowRight className="size-4 text-muted-foreground transition group-hover:translate-x-0.5"/></div><p className="mt-5 text-base font-semibold">{label}</p><p className="mt-1 text-sm leading-6 text-muted-foreground">{description}</p></Link>)}</section>{!actions.length&&<div className="rounded-2xl bg-muted/40 p-8 text-center text-sm text-muted-foreground">No tienes permisos disponibles en este módulo.</div>}</div>}
