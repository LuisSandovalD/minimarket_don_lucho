import{db}from"@/lib/db";import{requirePermission}from"@/lib/auth";import{money}from"@/lib/utils";
export const dynamic="force-dynamic";export default async function PurchasesPage(){await requirePermission("purchases.view");const rows=await db.purchase.findMany({include:{supplier:true,user:true},orderBy:{purchasedAt:"desc"},take:100});return <div className="space-y-6"><div><h1 className="text-2xl font-semibold">Compras</h1><p className="text-sm text-[var(--muted)]">Ingresos de mercadería por proveedor.</p></div><div className="overflow-x-auto rounded-xl bg-[var(--card)] shadow-sm"><table className="w-full text-sm"><thead><tr className="text-left"><th className="p-3">Código</th><th className="p-3">Fecha</th><th className="p-3">Proveedor</th><th className="p-3 text-right">Total</th><th className="p-3">Estado</th></tr></thead><tbody>{rows.map(x=><tr className="border-t" key={x.id}><td className="p-3">{x.code}</td><td className="p-3">{x.purchasedAt.toLocaleDateString("es-PE")}</td><td className="p-3">{x.supplier.tradeName||x.supplier.legalName}</td><td className="p-3 text-right">{money(x.total.toString())}</td><td className="p-3">{x.status}</td></tr>)}</tbody></table>{!rows.length&&<p className="p-8 text-center text-sm text-[var(--muted)]">No existen compras.</p>}</div></div>}
